# 部署环境变量说明

本项目已将数据库、邮箱、JWT、AI 服务和数字人相关密钥改为环境变量注入。生产部署时，请复制根目录 `.env.example` 的键名，在服务器或 CI 环境中填入真实值。

## 本地开发最小配置

默认 `dev` 环境已经内置本地开发用的数据库地址、用户名、CORS 白名单和 JWT 开发密钥。只要你的 MySQL 用户是 `root`，数据库名是 `intelliview_szmp`，本地启动只需要配置数据库密码：

```powershell
$env:DB_PASSWORD="你的MySQL密码"
mvn spring-boot:run
```

如果你的 MySQL 密码是 `123456`：

```powershell
$env:DB_PASSWORD="123456"
```

如果你的数据库用户名、端口或库名不是默认值，再额外配置 `DB_USERNAME`、`DB_PORT`、`DB_NAME`。

## 必填变量

生产环境建议显式配置以下变量：

| 变量 | 说明 |
|---|---|
| `DB_URL` | MySQL 连接串 |
| `DB_USERNAME` | 数据库用户名 |
| `DB_PASSWORD` | 数据库密码 |
| `JWT_SECRET` | JWT 签名密钥，建议不少于 64 个字符 |
| `FRONTEND_ORIGIN` | 前端访问源，例如 `http://localhost:5173` 或生产域名 |
| `VITE_API_BASE_URL` | 前端请求后端 API 的基础地址 |

## 按功能启用的变量

| 变量 | 说明 |
|---|---|
| `MAIL_HOST` / `MAIL_PORT` | 邮箱 SMTP 服务 |
| `MAIL_USERNAME` / `MAIL_PASSWORD` | 邮箱账号与授权码 |
| `DASHSCOPE_API_KEY` / `DASHSCOPE_APP_ID` | 阿里云百炼、语音识别与语音合成配置 |
| `IFLYTEK_AVATAR_APP_ID` / `IFLYTEK_AVATAR_API_KEY` / `IFLYTEK_AVATAR_API_SECRET` | 讯飞数字人服务鉴权 |
| `IFLYTEK_AVATAR_SCENE_ID` / `IFLYTEK_AVATAR_RESOURCE_ID` / `IFLYTEK_AVATAR_VCN` | 数字人场景、形象和音色配置 |

## 安全注意事项

- 不要把真实 `.env`、服务器环境变量截图、数据库转储或第三方密钥提交到 Git。
- 生产环境不要使用空的 `JWT_SECRET`，否则登录态签名不可用且存在安全风险。
- CORS 只配置可信前端域名；新增域名时使用 `FRONTEND_ORIGIN_2` 或在部署平台扩展同名配置。
- 简历文件不建议公开直连访问；用户应通过登录后的个人中心管理和删除简历。

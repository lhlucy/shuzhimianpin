# 数智面聘（Intelliview）项目简历分析

> 审查范围：`Intelliview-backend/`、`Intelliview-frontend-web/`、根目录 SQL、项目说明、部署说明、隐私说明与后端 API 文档。本文只陈述代码或文档可证实的事实；不包含任何密码、Token、API Key 或个人简历数据。状态标识为：**已实现**、**部分实现**、**未实现**、**需要人工确认**。

## 1. 项目概览

- **项目名称**：数智面聘（代码工程名 `Intelliview`）。证据：`Intelliview-backend/pom.xml` 的 `name`/`description`，`项目介绍.md`。
- **项目定位**：面向求职者的 AI 模拟面试与技术面试训练平台，围绕岗位、简历、技术栈和练习记录组织个性化面试，并提供题库练习、报告、成长分析及管理端。**已实现为主**；证据：`AIInterviewController`、`AIInterviewServiceImpl`、前端 `src/pages/user/AIInterview/*` 与 `项目介绍.md`。
- **核心功能（已实现）**：邮箱/密码/GitHub 登录，JWT 鉴权；题库、分类、标签、收藏、点赞、评论、练习历史；简历上传与 PDF/DOCX/TXT/MD 文本解析；AI 面试创建、逐题回答、AI 评估和追问、总结与成长分析；语音转写/合成、实时 ASR WebSocket、讯飞数字人会话；管理端题目、岗位、规则、用户、面试记录、Excel 导入导出与看板接口。
- **实际技术栈**：
  - 后端：Java 17、Spring Boot 2.7.15、Spring Security、JWT、MyBatis-Plus 3.5.3、MySQL、Redis、WebSocket、Swagger/Knife4j。用于 REST API、无状态认证、持久化、验证码与实时语音通道。证据：`Intelliview-backend/pom.xml`、`config/SecurityConfig.java`。
  - 前端：Vue 3、TypeScript、Vite、Vue Router、Element Plus、Axios、ECharts、Markdown-it、DOMPurify。用于用户/管理端页面、路由、接口访问、图表与富文本显示。证据：`Intelliview-frontend-web/package.json`、`src/router/index.ts`。
  - AI/第三方：阿里云百炼应用、阿里云 ASR/TTS、讯飞数字人、GitHub OAuth、邮件服务。均有调用或配置代码，是否在生产环境实际启用需人工确认。证据：`service/impl/AliyunBailianAppServiceImpl.java`、`AIInterviewSpeechServiceImpl.java`、`AIInterviewAvatarServiceImpl.java`、`GithubAuthService.java`。
  - Elasticsearch：**部分实现**。存在依赖、实体、同步服务与定时任务，但默认配置 `spring.elasticsearch.enabled: false`，是否部署和启用需确认。证据：`pom.xml`、`application.yml`、`ElasticsearchSyncService.java`。
- **项目完成度**：核心 AI 面试闭环、题库训练、账号和管理接口已落地；质量保障、分布式稳定性、可观测性和生产运行数据不足。综合判断为**可演示的完整原型/课程竞赛项目，工程化生产能力部分实现**。旅游规划能力：**不适用**（未发现地图、行程、景点、路线或旅游领域代码）。

## 2. 核心业务流程

### 流程 A：个性化 AI 面试创建

用户在前端选择岗位、技术栈、练习题与简历内容 → `POST /api/interviews/ai/personalized` → `AIInterviewController.createInterview` → `AIInterviewServiceImpl.createInterview` 读取当前用户、岗位与历史练习题，调用 `createInterviewBlueprint` 构建面试蓝图 → 写入面试主记录和题目记录 → 返回面试详情。

- 数据表：`ai_interviews`、`ai_interview_questions`、`job_roles`、`job_role_skill_dimensions`、`questions`、`user_practice_history`。
- 保护：创建频控、客户端请求 ID 复用和事务。`AIInterviewStabilityGuard` 使用 JVM `ConcurrentHashMap` 实现一分钟窗口限流与 30 分钟（可配置）幂等记录；`createInterview` 标注 `@Transactional`。**已实现，但仅单实例有效**。
- 主要证据：`controller/AIInterviewController.java#createInterview`，`service/impl/AIInterviewServiceImpl.java#createInterview`，`service/impl/AIInterviewStabilityGuard.java`，`intelliview_szmp.sql` 的上述建表定义。

### 流程 B：作答、AI 评分、动态追问与完结

用户提交文字或语音转写后的答案 → `POST /api/interviews/ai/{interviewId}/questions/{questionId}/answers` → `AIInterviewServiceImpl.submitAnswer` 校验面试归属、状态和题目 → `evaluateAnswerWithAI` 构建 Prompt 并调用百炼 → 从模型文本抽取 JSON，以 Jackson 解析分数、五维评分、关键词、优缺点与建议 → 写入答案；若满足追问规则则生成追问，否则取下一题；无下一题时完成面试并生成汇总。

- 数据表：`ai_interview_answers`（反馈、关键词、表达分析为 JSON 字段）、`ai_interview_questions`、`ai_interviews`、`ai_interview_assessments`、`user_interview_history`。
- 状态：`PENDING → IN_PROGRESS → COMPLETED`；提交非进行中面试会被拒绝，同一面试题已作答会被拒绝。**已实现**。
- 主要证据：`AIInterviewServiceImpl#startInterview`、`#submitAnswer`、`#evaluateAnswerWithAI`、`#finishInterview`，`AIInterviewController#submitAnswer`，`intelliview_szmp.sql`。

### 流程 C：简历上传/解析与面试上下文

用户上传简历 → `POST /api/user/resumes` 或 `/api/interviews/ai/resume/parse` → `UserResumeServiceImpl.uploadResume` / `AIInterviewServiceImpl.parseResume` → `ResumeParseService.parse` 用 PDFBox 或 Apache POI 提取 PDF/DOCX 文本，或读取 TXT/MD，限制内容长度并按规则抽取意向岗位、城市、薪资及求职类型 → 简历文件本地存储，解析正文经 `ResumeCryptoService` 加密后写入数据库（用户简历上传链路）。

- 数据表：`user_resumes`；面试创建时简历摘要/内容进入 `ai_interviews` 的快照字段。
- **已实现**：格式校验、解析失败提示、所有权校验与删除；文件限制由 Spring Multipart 配置为 5MB，语音单独限制 10MB。
- 主要证据：`UserResumeController`，`UserResumeServiceImpl#uploadResume/#deleteResume`，`ResumeParseService#parse/#extractText`，`application.yml`。

### 流程 D：实时语音与数字人交互

前端建立携带 JWT 的 `/ws/ai/asr/realtime` WebSocket → `AIInterviewRealtimeAsrWebSocketHandler` 验证 Token → 建立到阿里云实时 ASR 的 WebSocket，将浏览器二进制音频转发并把 partial/final/error 事件回传；前端亦可请求 TTS 和数字人会话 → `AIInterviewSpeechServiceImpl` HTTP 调用 ASR/TTS，`AIInterviewAvatarServiceImpl` 与讯飞数字人 WebSocket 交互并维持心跳、超时清理和文字/语音降级。

- 不涉及业务表的实时转写流；数字人会话在应用内存保存。
- **已实现但依赖外部配置**：缺少密钥或初始化失败时数字人返回文字/语音模式回退；生产可用性需人工确认。
- 主要证据：`websocket/AIInterviewRealtimeAsrWebSocketHandler.java`，`AIInterviewSpeechServiceImpl`，`AIInterviewAvatarServiceImpl#initSession/#speak`，前端 `src/pages/user/AIInterview/AIInterviewSession.vue`、`src/components/AIInterview/AvatarDigitalHumanPlayer.vue`。

## 3. 后端技术分析

| 主题 | 结论与代码证据 | 简历适配性 |
|---|---|---|
| Controller / Service / Mapper 分层 | **已实现**。Controller 接收 HTTP 请求，Service 编排业务，Mapper 继承 MyBatis-Plus `BaseMapper`；AI、题库、用户、管理端均按层组织。例：`AIInterviewController → AIInterviewServiceImpl → AIInterviewMapper/AIInterviewQuestionMapper`。 | 适合写“基于 Spring Boot + MyBatis-Plus 构建分层业务模块”，不宜夸大为微服务。 |
| DTO / VO / Entity | **已实现**。`dto/request`（如 `AIInterviewCreateRequest`）、`dto/response`（如 `AIInterviewResponse`、`ApiResponse`）与 `entity` 分离；前端也有 `src/api/*` 和页面模型。 | 适合写“请求/响应对象隔离持久化实体，降低接口耦合”。 |
| 参数校验 | **部分实现**。多处关键请求使用 `@Valid`，DTO 有 Bean Validation；全局处理 `MethodArgumentNotValidException`。但 `BankController`、`PaperController`、部分管理员更新接口直接接收 Entity/未标注 `@Valid`。 | 可写“关键接口接入 Bean Validation”，需坦承并非全覆盖。 |
| 统一返回 / 异常 | **已实现**。接口普遍返回 `ResponseEntity<ApiResponse<T>>`；`GlobalExceptionHandler` 统一处理验证、认证、业务、运行时与系统异常。 | 适合写，但不要声称所有异常具备精细 HTTP 状态映射。 |
| 登录鉴权 / 权限 | **已实现**。`SecurityConfig` 配置无状态会话，`JwtAuthFilter` 从 Bearer Token 填充安全上下文，密码使用 BCrypt，管理端由 URL 规则与 `@PreAuthorize("hasAuthority('ROLE_ADMIN')")` 双重约束。 | 强烈适合写。 |
| 分页查询 | **已实现**。题目、回答、评论、收藏、报告、搜索等接口返回 MyBatis-Plus `IPage`；见 `QuestionController`、`AnswerController`、`ReportController`。 | 适合写“对列表接口使用 MyBatis-Plus 分页与条件查询”。 |
| 事务 | **已实现为主**。面试创建/答题、简历、题目、评论、收藏、点赞、报告和管理端服务多处使用 `@Transactional`。 | 适合写具体多表场景，例如“面试主记录与题目批量落库使用事务”。 |
| 状态流转 | **已实现**。AI 面试以 `PENDING/IN_PROGRESS/COMPLETED` 控制开始与提交；报告还有处理状态；数据库有状态字段。面试流转未抽成独立状态机。 | 可写“实现状态校验”，不宜称“状态机引擎”。 |
| 幂等 / 重复提交 | **部分实现**。创建面试支持 `clientRequestId` 重用；答案以“面试 ID + 题目 ID 已存在”阻止重复作答。前者为 JVM 内存实现，数据库未见该组合唯一约束，分布式/并发一致性不足。 | 可写“实现单实例幂等与重复作答拦截”，不要写分布式幂等。 |

## 4. 数据库与 Redis

### 主要表与关联

- 用户与认证：`users`、`verification_codes`、`login_records`、`messages`。`users` 有 `username`、`email`、`github_id` 唯一索引，`role` 与 `enabled` 索引；验证码有 `(email,type)`、过期时间和已使用标志索引。**已实现**。证据：`intelliview_szmp.sql`。
- AI 面试闭环：`ai_interviews`（用户、岗位、状态、配置快照）、`ai_interview_questions`、`ai_interview_answers`、`ai_interview_assessments`、`user_interview_history`、`user_growth_snapshots`。答案表按 `interview_id`、`question_id`、提交时间建立索引，反馈相关字段使用 MySQL JSON。**已实现**。
- 题库与互动：`questions`、`question_banks`、`categories`、`tags`，以及题目-标签、题目-岗位、题目-能力维度、题库-分类等关联表；另有 `answers`、`comments`、`like_records`、`user_favorites`、`user_practice_history`。**已实现**。
- 岗位/规则：`job_roles`、`job_role_skill_dimensions`、`interview_templates`、`interview_follow_up_rules`、`evaluation_calibration`。**已实现**；规则是否在所有生成/评估路径充分生效需要人工确认。
- 简历：`user_resumes` 存文件元数据、解析文本及意向字段；代码通过 `ResumeCryptoService` 对正文加密。**已实现**。SQL 转储包含用户样例和业务数据，简历、邮箱等敏感数据不应提交或传播（详见改进建议）。

### 索引、约束与事务

- **已实现**：主键、部分外键、用户唯一索引、面试答案的常用查询索引、验证码查询索引。
- **部分实现**：关联表与高频查询表的完整唯一约束不均衡。以 AI 答案为例，代码阻止重复 `(interview_id, question_id)`，但 SQL 表中仅有普通索引，未见组合唯一约束；并发下仍可能绕过应用层判断。
- **已实现**：面试创建同时写主表和题目、答题写答案并推进流程均位于事务方法；见 `AIInterviewServiceImpl#createInterview/#submitAnswer`。

### Redis 实际使用

- **已实现且可确认的场景仅为图形验证码**：`CaptchaService` 使用 `StringRedisTemplate.opsForValue()` 写入 `captcha:{uuid}`，String 类型，TTL 来自 `app.captcha.expire-seconds`，默认 300 秒；校验后立即删除，实现一次性验证码。证据：`CaptchaService#generateCaptcha/#validateCaptcha`、`application.yml`。
- **未实现/未发现**：题库热点缓存、AI 结果缓存、分布式限流、分布式幂等、会话共享等 Redis 使用；`AIInterviewStabilityGuard` 明确采用本地 `ConcurrentHashMap`。因此不应把 Redis 描述为“缓存题库/AI 结果”或“分布式限流”。
- **缓存一致性**：当前无业务数据缓存，故无“缓存-数据库双写一致性”问题；验证码是短生命周期一次性数据，校验即删。若后续加入缓存，需要处理题目/标签编辑后的失效策略。

## 5. AI 与第三方服务

- **百炼应用（已实现）**：`AliyunBailianAppServiceImpl#chat` 以 Java `HttpClient` 发送 JSON：`input.prompt`，可选 `parameters.session_id`；解析响应的 `output.text`、`output.session_id`、`request_id`。`AIInterviewServiceImpl#evaluateAnswerWithAI` 从文本中提取 JSON 对象，用 Jackson 读取 `score`、`dimensionScores`、关键词、优缺点、建议和追问字段。多轮上下文：云知识接口支持 `session_id`；AI 面试评估主要由当前面试/题目/答案拼 Prompt，未见持久化的完整模型会话历史。 
- **AI 题目与评估（已实现）**：岗位、技术栈、练习记录、简历内容进入创建蓝图/评估 Prompt；回答输出被结构化保存到答案 JSON 字段。维度模型有单元测试，验证限幅与缺省回退：`AIInterviewDimensionModelTest`。
- **超时/异常（部分实现）**：百炼、ASR/TTS 和数字人配置了 connect/read timeout，业务层捕获异常并返回用户可读错误；数字人初始化失败回退到文字/语音模式。**未实现/未发现**通用重试、熔断、异步任务队列、调用审计指标或模型结果降级缓存。AI 调用为同步阻塞式 `HttpClient.send`。
- **语音（已实现）**：`AIInterviewSpeechServiceImpl#transcribe` 将音频转 data URI 提交 ASR，限制 10MB；`#synthesize` 请求 TTS 并返回音频 URL。`AIInterviewRealtimeAsrWebSocketHandler` 双向桥接浏览器和阿里云实时识别服务，回传 partial/final 事件。
- **数字人（已实现但需配置）**：`AIInterviewAvatarServiceImpl` 创建讯飞 WebSocket 会话、签名请求、心跳与过期清理；达到会话上限、未配置或连接失败时回退。会话 Map 和定时线程仅在当前 JVM 内，集群部署时不共享。 
- **其他第三方（已实现）**：GitHub OAuth（`GithubAuthService`）、邮件验证码（`EmailService`/`AuthController`）。地图、旅游、支付：**不适用/未发现**。

## 6. 最有技术含量的内容

1. **岗位/简历驱动的 AI 面试编排（已实现）**
   - 解决的问题：通用问答缺少岗位和个人经历上下文。
   - 实现方式：读取岗位、技能维度、已练习题与简历文本，生成面试蓝图并落库为配置快照及题目序列。
   - 代码证据：`AIInterviewServiceImpl#createInterview/#createInterviewBlueprint`，`JobRole`、`JobRoleSkillDimension`、`ai_interviews`、`ai_interview_questions`。
   - 简历推荐写法：**“设计岗位、技术栈、练习历史与简历上下文驱动的 AI 模拟面试编排，事务化持久化面试快照及题目序列。”**
   - 面试官可能追问：上下文如何限长？题目如何避免重复？生成失败如何处理？

2. **结构化 AI 回答评估与动态追问（已实现）**
   - 解决的问题：将模型自由文本转为可展示、可聚合的面试反馈。
   - 实现方式：Prompt 要求 JSON，提取 JSON 后以 Jackson 解析五维评分、关键词、优缺点与建议；根据评估结果决定追问、下一题或结束。
   - 代码证据：`AIInterviewServiceImpl#submitAnswer/#evaluateAnswerWithAI/#shouldCreateFollowUp`，`AIInterviewDimensionModel`。
   - 简历推荐写法：**“实现 LLM JSON 结构化解析与多维回答评估，驱动动态追问和面试状态推进。”**
   - 面试官可能追问：模型返回非 JSON 怎么处理？分数如何校准？如何避免重复追问？

3. **AI 面试核心链路的事务、限流与幂等（部分实现）**
   - 解决的问题：创建/作答重复提交与多表半写入。
   - 实现方式：创建和答题方法加 `@Transactional`；客户端请求 ID 缓存面试 ID；用户/全局创建和作答限流；题目已作答检查。
   - 代码证据：`AIInterviewServiceImpl#createInterview/#submitAnswer`，`AIInterviewStabilityGuard`。
   - 简历推荐写法：**“为 AI 面试创建与作答链路加入事务、单实例限流、请求幂等和重复作答拦截。”**
   - 面试官可能追问：为何不是 Redis？多实例如何保证原子性？数据库唯一约束如何补？

4. **实时 ASR WebSocket 桥接（已实现）**
   - 解决的问题：浏览器录音需要低延迟地获得增量转写而非等待完整文件上传。
   - 实现方式：校验 JWT 后接入客户端二进制音频，转发至阿里云实时 ASR WebSocket，回传 partial/final/error 事件，断开时关闭上游连接。
   - 代码证据：`AIInterviewRealtimeAsrWebSocketHandler`。
   - 简历推荐写法：**“实现 JWT 鉴权的 WebSocket 实时 ASR 代理，桥接浏览器音频流与云端识别服务并回传增量/最终转写。”**
   - 面试官可能追问：音频编码、背压、断线重连与 Token 放在 query 的风险如何处理？

5. **语音与数字人服务降级（部分实现）**
   - 解决的问题：外部数字人不可用不应阻塞文字/语音面试。
   - 实现方式：数字人连接配置、会话配额、初始化超时、心跳和清理；异常返回可降级提示，继续文字/语音流程。
   - 代码证据：`AIInterviewAvatarServiceImpl#initSession/#fallbackSession/#scheduleHeartbeat`，`AIInterviewSpeechServiceImpl`。
   - 简历推荐写法：**“接入云端 TTS/ASR 与数字人 WebSocket，会话初始化失败时降级至文字/语音面试。”**
   - 面试官可能追问：会话如何跨实例？连接池与资源回收如何做？

6. **简历文本解析与加密存储（已实现）**
   - 解决的问题：把用户上传的多格式简历转为可用于面试定制的文本，同时降低正文直接落库暴露。
   - 实现方式：PDFBox/Apache POI 提取 PDF/DOCX，支持 TXT/MD，限长并规则抽取意向字段；简历正文经 `ResumeCryptoService` 加密。
   - 代码证据：`ResumeParseService#parse/#extractText`，`UserResumeServiceImpl#uploadResume`，`ResumeCryptoService`。
   - 简历推荐写法：**“实现 PDF/DOCX/TXT/MD 简历解析、内容限长与敏感正文加密存储，作为个性化面试上下文。”**
   - 面试官可能追问：密钥轮换、文件病毒扫描、OCR 扫描件如何处理？

7. **JWT + RBAC 鉴权体系（已实现）**
   - 解决的问题：区分公开题库、登录用户和管理员后台的访问边界。
   - 实现方式：Spring Security 无状态会话，JWT Filter 加载用户权限，BCrypt 密码哈希，URL 和方法级 `ROLE_ADMIN` 限制。
   - 代码证据：`SecurityConfig`，`JwtAuthFilter`，`JwtService`，管理端 Controller 的 `@PreAuthorize`。
   - 简历推荐写法：**“基于 Spring Security + JWT 构建无状态认证与 RBAC 权限控制，保护用户、AI 面试及管理端接口。”**
   - 面试官可能追问：Token 刷新/注销如何失效？权限变更后 Token 如何处理？

8. **题库训练与运营后台（已实现）**
   - 解决的问题：为 AI 面试提供可管理的内容来源与用户练习数据。
   - 实现方式：题目/题库/标签/分类/岗位 CRUD、分页筛选、收藏评论点赞、练习历史和 Excel 导入导出。
   - 代码证据：`QuestionService`、`UserPracticeHistoryServiceImpl`、`service/admin/ExcelImportExportService`、对应 Controller。
   - 简历推荐写法：**“构建可分页筛选的题库训练与管理后台，沉淀用户练习历史并向 AI 面试提供题目上下文。”**
   - 面试官可能追问：题目导入如何校验？统计查询如何优化？

## 7. 项目问题与改进建议

1. **[高] SQL 转储含真实业务/个人数据，且开发配置存在明文本地凭据。** 证据：`intelliview_szmp.sql` 有插入记录，`application-dev.yml` 有开发默认连接信息。改进：移除转储中的记录或以脱敏 seed 替代；开发密码也改为环境变量；执行密钥轮换并用 Git 历史清理工具处理已提交敏感信息。
2. **[高] AI 限流和幂等仅在单 JVM 内生效。** 证据：`AIInterviewStabilityGuard` 使用 `ConcurrentHashMap`。改进：以 Redis + Lua 或数据库唯一键实现原子限流/幂等；为 `ai_interview_answers(interview_id,question_id)` 添加唯一约束。
3. **[高] LLM/ASR/TTS 采用同步阻塞调用，未见重试、熔断与异步化。** 证据：各服务使用 `HttpClient.send`；仅有 timeout 和 catch。改进：使用异步任务/消息队列处理长任务，接入 Resilience4j（超时、有限重试、熔断、舱壁），记录第三方 request ID、耗时和失败原因。
4. **[中] 全局异常将大部分业务/运行时错误映射为 HTTP 400。** 证据：`GlobalExceptionHandler`。改进：区分 401/403/404/409/422/429/5xx，向前端返回稳定错误码，避免把内部异常信息直接作为响应 message。
5. **[中] 参数校验覆盖不完整。** 证据：`BankController`、`PaperController` 部分方法直接接收 Entity，关键 AI/API 请求则使用 `@Valid`。改进：全面使用 Request DTO、`@Validated`、字段白名单和上传文件 MIME/内容校验。
6. **[中] 语音 WebSocket 的 JWT 放在查询参数，可能出现在代理/访问日志。** 证据：`AIInterviewRealtimeAsrWebSocketHandler#resolveQuery`。改进：改用 Sec-WebSocket-Protocol、短期一次性 WebSocket ticket，或在握手 Header 中传递并对日志脱敏。
7. **[中] 数字人会话和定时线程为本地内存实现，集群不可共享。** 证据：`AIInterviewAvatarServiceImpl` 的 `ConcurrentMap`、`ScheduledExecutorService`。改进：会话状态迁移至 Redis/专用会话服务，统一生命周期和资源指标；应用关闭时优雅停止线程池。
8. **[中] Elasticsearch 依赖/代码存在但默认关闭。** 证据：`pom.xml`、`application.yml`、`ElasticsearchSyncService`。改进：若不计划启用则移除并避免简历夸大；若启用，补充索引初始化、增量同步、失败补偿、回滚和检索压测。
9. **[中] 自动化测试覆盖很少。** 证据：仅发现 Spring 上下文测试和 `AIInterviewDimensionModelTest` 两个测试类。改进：补 AI 面试创建/作答的 service 与 controller 集成测试、权限矩阵、文件解析、异常回退和 WebSocket 契约测试；以 H2/Testcontainers 隔离数据。
10. **[低] API 文档可能落后于当前功能。** 证据：`Intelliview-backend/doc/API文档.md` 以传统题库为主，当前代码另有完整 AI 面试、语音、数字人、岗位接口。改进：从 OpenAPI/Knife4j 注解自动生成或维护契约测试，发布前校验文档与路由一致。

## 8. 可量化数据建议

以下均为可实际采集/压测的指标，不代表现有结果：

- AI 面试创建、提交答案、查询总结的平均响应时间、P95/P99、错误率与 QPS。
- 百炼题目生成和回答评估的平均/分位调用耗时、超时率、非 JSON 返回率、JSON 解析成功率、降级率。
- ASR：端到端首个 partial、最终转写耗时、断连率、识别失败率；TTS：合成耗时和 URL 可用率。
- 数字人：初始化成功率、平均初始化时长、心跳断开率、降级比例、并发会话数。
- 题库：分页查询响应时间、索引命中情况、管理端导入耗时与失败行数。
- Redis：验证码读写成功率、平均延迟、Key 数和 TTL 分布；若后续增加缓存，再统计缓存命中率和失效延迟。
- 数据质量：每次面试平均题数、完成率、有效评分率、平均追问次数（需埋点后统计）。
- 质量保障：单元/集成/E2E 测试用例数、核心链路覆盖率、缺陷修复时长。

## 9. 简历项目经历草稿

### 数智面聘｜AI 模拟面试与技术面试训练平台

- **一句话项目介绍**：面向求职训练场景的前后端分离平台，结合岗位、简历和练习记录生成个性化 AI 面试，并提供语音/数字人交互、结构化评估、成长分析和题库管理。
- **核心技术栈**：Java 17、Spring Boot、Spring Security、JWT、MyBatis-Plus、MySQL、Redis、Vue 3、TypeScript、WebSocket、阿里云百炼/ASR/TTS、讯飞数字人。
- **技术要点**：
  - 基于 Spring Boot + MyBatis-Plus 设计 Controller/Service/Mapper 分层，使用 DTO/统一响应/全局异常处理支撑题库、训练、报告及管理端接口。
  - 基于 Spring Security + JWT 实现无状态认证与 RBAC，结合 BCrypt、URL 规则和方法级 `ROLE_ADMIN` 控制用户与后台资源访问。
  - 实现岗位、技术栈、练习历史和简历上下文驱动的 AI 面试编排，使用事务写入面试快照及题目序列。
  - 构建 LLM 结构化评估链路：解析 JSON 格式的多维评分、关键词和反馈，基于结果触发动态追问并推进面试状态。
  - 实现 JWT 鉴权的实时 ASR WebSocket 桥接，以及云端 ASR/TTS 与数字人会话集成；数字人初始化失败时回退文字/语音模式。
  - 使用 Redis 存储一次性图形验证码（默认 300 秒 TTL），并在 AI 面试创建/答题中实现单实例频控、请求幂等和重复作答拦截。
- **项目成果**：**需要补充量化数据**。代码可证明功能闭环与工程实现，但无法证明用户规模、性能成绩、上线效果或获奖情况；不要在简历中填入未经实测的数字。
- **需要补充的量化数据**：接口 P95、AI 调用耗时/成功率、ASR 延迟、面试完成数/完成率、测试覆盖率、部署环境与实际用户数。

> 使用建议：若本人没有负责全部模块，应将上述动词改为“负责/参与/协助”，并只保留自己能解释代码细节的条目。

## 10. 需要我人工确认的信息

- 团队人数、项目周期、本人角色与实际负责模块分别是什么？哪些核心代码由本人独立完成？
- 是否已部署到可访问环境？部署环境、域名、云资源、CI/CD、监控和告警是否真实存在？
- 阿里云百炼、ASR/TTS、讯飞数字人与 GitHub OAuth 是否已经配置真实凭据并成功联调？生产环境分别是否启用？
- 是否有真实用户、试用对象或高校/课程场景？用户数量、完成面试次数与反馈如何取得？
- 是否做过接口压测、前端性能测试、兼容性测试、安全测试或真实评测？请提供原始报告/日志，而非口头估计。
- 是否获得比赛奖项、软著、专利、校级立项或公开展示？名称、时间和证书信息需以可核验材料为准。
- 数据库 SQL 中的样例/个人数据是否已从仓库与 Git 历史清理？真实简历和音频的授权、保存期限、删除机制是否已落实？
- Elasticsearch 是否计划启用？若没有，简历中应不列为“实际使用”；若有，请确认索引和同步是否已部署验证。
- AI 编程学习平台：**部分适用**（题库、岗位能力模型、技术面试训练已实现），但未见在线代码运行、判题沙箱、课程/代码评测等实现。
- 智能旅游规划项目：**不适用**（未发现地图、POI、路线规划、行程生成或旅游第三方接口实现）。
